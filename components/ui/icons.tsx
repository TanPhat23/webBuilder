import { FacebookIcon, LucideProps } from "lucide-react";
import { Loader2, Github, Mail } from "lucide-react";

export type IconProps = LucideProps;

export const Icons = {
  spinner: Loader2,
  gitHub: Github,
  google: Mail,
  facebook: FacebookIcon,
  mail: Mail,
};
