export interface buttonOnClickEvent {
  selectedElementId: string;
  selectedComboboxElementId: string;
  number: number;
}
export interface buttonOnHoverEvent {
  selectedElementId: string;
  backColor: string;
  textColor: string;
}
